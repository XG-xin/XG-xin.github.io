import type { EndpointBuilder } from '@datadog/browser-core';
import type { SegmentMetadata } from '../types';
export declare const SEND_BEACON_BYTE_LENGTH_LIMIT = 60000;
export declare function send(endpointBuilder: EndpointBuilder, data: Uint8Array, metadata: SegmentMetadata, rawSegmentSize: number, flushReason?: string): void;
export declare function toFormEntries(input: object, onEntry: (key: string, value: string) => void, prefix?: string): void;
