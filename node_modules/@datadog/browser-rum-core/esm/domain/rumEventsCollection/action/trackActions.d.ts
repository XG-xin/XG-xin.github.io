import type { Context, Duration, ClocksState, Observable, RelativeTime } from '@datadog/browser-core';
import { ActionType } from '../../../rawRumEvent.types';
import type { RumConfiguration } from '../../configuration';
import type { LifeCycle } from '../../lifeCycle';
declare type AutoActionType = ActionType.CLICK;
export interface ActionCounts {
    errorCount: number;
    longTaskCount: number;
    resourceCount: number;
}
export interface CustomAction {
    type: ActionType.CUSTOM;
    name: string;
    startClocks: ClocksState;
    context?: Context;
}
export interface AutoAction {
    type: AutoActionType;
    id: string;
    name: string;
    startClocks: ClocksState;
    duration: Duration;
    counts: ActionCounts;
    event: Event;
}
export interface AutoActionCreatedEvent {
    id: string;
    startClocks: ClocksState;
}
export interface ActionContexts {
    findActionId: (startTime?: RelativeTime) => string | string[] | undefined;
}
export declare const AUTO_ACTION_MAX_DURATION: number;
export declare const ACTION_CONTEXT_TIME_OUT_DELAY: number;
export declare function trackActions(lifeCycle: LifeCycle, domMutationObservable: Observable<void>, { actionNameAttribute }: RumConfiguration): {
    stop: () => void;
    actionContexts: ActionContexts;
};
export {};
